#!/bin/bash

tools=/home/users/phtof/tools
sudo=
kernel_dir="/ssd/philippos/github/philipos-5.13/linux-5.13"
userspace_dir="/ssd/philippos/github/philipos-5.13/userspace/src"
perf="$kernel_dir"/tools/perf/perf
# Ugly and complicated way to get the absolute path to this script
scripts_dir="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 ; pwd -P)"
base_dir=$scripts_dir/..
mountpoint=/mnt/pmem
workdir=$mountpoint/phtof
fiobin=$tools/fio/fio

# Colours for fancy printing
RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m' # No Color
